#include "main.h"
#include <string.h>

/**
*Function that returns the length of a string
*int _strlen_recursion; function that checks the length of string
*(char *s); the string parameter received
*/
int _strlen_recursion(char *s)
{
return (strlen(s));
}
