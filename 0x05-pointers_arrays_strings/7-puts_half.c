#include "main.h"
/**
 * prints_half - printing half a string
 * @str: that's the string
 *
 * Return: void
 */
void puts_half(char *str)
{
int length = 0;

while (*str)
length++;
str++;
}
length  += 1;
start += length / 2;
while(*start)
{
putchar(*(start));
start++;
}
putchar('\n');
}
