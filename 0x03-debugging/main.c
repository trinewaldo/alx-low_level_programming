#include "main.h"

/**
 * main - Tests a function printing if an integer is positive or negative.
 *
 * Return: Always returns 0.
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
