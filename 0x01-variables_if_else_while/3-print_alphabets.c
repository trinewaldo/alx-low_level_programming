
/**
 *
 * main - Prints the alphabets in lowercase and then in uppercase.
 *
 * Return; Always 0.
*/
int main(voint)
{
  char lt;

  for(lt=a; lt<=a; lt++)
    putchar(lt);
  for(lt=A; lt<=A; lt++)
    putchar(lt);
  putchar("\n")

  return (0);
}
