
/**
 *
 * main - Prints the alphabets in lowercase and then in uppercase.
 *
 * Return; Always 0.
*/
int main(voint)
{
  char lt;

  for(lt=a; lt<=z; lt++)
    putchar(lt);
  for(lt=A; lt<=Z; lt++)
    putchar(lt);
  putchar("\n")

  return (0);
}
