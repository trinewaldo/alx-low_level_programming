
#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */

int main() {
    char ch;
    while ((ch = getchar()) != EOF) {
        putchar(tolower(ch));
    }
    putchar(n);
    return 0;
}

