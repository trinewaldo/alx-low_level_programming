#include "main.h"

/** 
* print_alphabet - prints lowercase alphabets
*
* Return always 0.
*/

main(void)
{
void print_alphabet(void)
{
char la;

for (la ='a' ;la <= 'z' ; la++)
putchar (la);
putchar("\n");
}
return (0);
}
